#include <vector>
#include <iostream>
#include <stdio.h>

using namespace std;

int main(int argc, char** argv) {
  int zahl = 0;
  while (zahl < 10) {
    cout << "Hallo" << endl;
    zahl = zahl + 1;
    cout << zahl << endl;
  }
}

